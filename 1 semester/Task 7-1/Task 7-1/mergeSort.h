#pragma once
#include "list.h"

// Рекурсивное деление списков 
LinkedList* mergeSort(LinkedList* list, bool isSortPhone);

// Слияние отсортированных списков 
//LinkedList* merge(LinkedList* listFirst, LinkedList* listSecond, bool isSortPhone);